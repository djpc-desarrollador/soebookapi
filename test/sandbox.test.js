describe(
    "Pruebas de la aplicacion: SoeBookApi",
    () => {
        test(
            "Guardado de nuevo libro",
            () => {
                var result = false;

                expect(result).toBe(true);
            }
        )
    }
);